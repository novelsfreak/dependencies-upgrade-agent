# dependencies-upgrade-agent
An agent that watches repositories, decides when a dependency should be upgraded, performs the upgrade including breaking API changes, proves it works, opens a PR, and then responds to CI failures and human review comments. Runs unattended.
